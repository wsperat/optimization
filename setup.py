from setuptools import find_packages, setup

setup(
    name="src",
    packages=find_packages(),
    version="0.1.0",
    description="The objective of this project is to study the relationship between the data and the target for othsupervised learning projects.",
)
